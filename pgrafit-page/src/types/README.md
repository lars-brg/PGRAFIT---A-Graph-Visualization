# 🏷️ Definições de Tipos (`src/types`)

Este diretório armazena todas as definições de tipos, interfaces e enums TypeScript que são usadas em múltiplos arquivos do projeto.

**Conteúdo Principal:**

- **Tipos de Dados do Grafo:** Interfaces que definem a estrutura dos dados que vêm da API Python.
  - Exemplo: `graph.ts` (Interfaces `GraphNode`, `GraphEdge`, `GraphData`).
- **Tipos de Componentes:** Tipos complexos usados como *props* em vários componentes.
- **Tipos de Resposta da API:** Estruturas de dados complexas retornadas pelos endpoints da API.