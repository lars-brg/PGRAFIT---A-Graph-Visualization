```markdown
# 🧩 Componentes Específicos da Aplicação (`src/components/app`)

Esta pasta contém componentes que são **específicos** para a visualização de grafos ou para a lógica de negócio principal. Eles geralmente dependem de outros dados do projeto (tipos customizados, hooks, funções de `lib`).

**Exemplos:**
- **`GraficoSigma.tsx`**: Onde o grafo é renderizado e configurado usando o `react-sigma`.
- **`FiltrosGrafo.tsx`**: Componente que gerencia a lógica de filtragem de dados.
- **`DadosEstatisticos.tsx`**: Exibe métricas e resumos do grafo.